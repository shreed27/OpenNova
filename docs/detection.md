# YOLO Detection Setup

1. Install the optional group:
   ```bash
   pip install -r requirements-detection.txt
   ```
2. Keep `yolov8n.pt` at the project root or update the detection skill to point to another model.
3. Grant camera permissions to your terminal app.
4. Run the GUI or text mode and ask JARVIS to detect objects.
